<div style="max-width: 420px; margin: 0 auto 8px auto;">
    <div class="today-facts">
        <p>Idag är det <b>{{#if today.helgdag}}today.helgdag{{/if}} {{#if today.helgdagsafton}}today.helgdagsafton{{/if}} {{#if today.redDay}}(röd dag){{/if}}</b></p>
        <p class="today-number">{{today.weekday}} {{today.month.name}} {{today.day}}</p>
        <p>vecka {{today.week}}</p>
        <br>
        {{#if today.namnsdag}}
            <div>
                {{#each today.namnsdag}}
                    <span>{{this}}</span>{{#unless @last}} &amp; {{/unless}}
                {{/each}}
                har namnsdag
            </div>
        {{/if}}

        {{#if today.themeDays}}
            <br>
            <div>
                Temadagar:
                {{#each today.themeDays}}
                    <p>{{this}}</p>
                {{/each}}
            </div>
        {{/if}}
    </div>
</div>

<div class="calendar calendar-grid">
  <h1 class="month">
    {{month.name}}
  </h1>

  <ol class="day-names">
    <li>Mån</li>
    <li>Tis</li>
    <li>Ons</li>
    <li>Tor</li>
    <li>Fre</li>
    <li class="weekend">Lör</li>
    <li class="weekend">Sön</li>
  </ol>

  <section class="weekNumbers">
    {{#each weekNumbers}}
      <div>v{{this}}</div>
    {{/each}}
  </section>

  <ol class="days">
    {{#each days}}
       <li class = "{{#ifnoteq month.name ../selectedMonth.month}}previousMonth{{/ifnoteq}}
        {{#if isToday}}isToday{{/if}}
        {{#if redDay}}redDay{{/if}}
        {{#if helgDag}}helgDag{{/if}}
        {{#ifeq veckodag 'Lördag'}}weekend{{/ifeq}} {{#ifeq veckodag 'Söndag'}}weekend{{/ifeq}}">
        <div class="date">
          {{dayNumber}} <span class="prevmonthname">{{#ifnoteq month.name ../selectedMonth.month}}{{month.name}}{{/ifnoteq}}</span>
        </div>
        <div class="holiday">
          {{helgdag}}
          {{helgdagsafton}}
        </div>
        <div class="namedays">
          {{#each namnsdag}}
            <span>{{this}}</span>{{#unless @last}} &amp; {{/unless}}
          {{/each}}
        </div>
        <div class="themedays">
            {{#each themeDays}}
                <p>{{this}}</p>
            {{/each}}
        </div>
      </li>
    {{/each}}
    <li>
  </ol>
</div>
<br>
<div class="menu-bar">
    <a style="float: left" class="button" href="/showCalendar?year=2018&amp;month=10">Föregående månad</a>
    <a style="float: right" class="button" href="/showCalendar?year=2018&amp;month=12">Nästa månad</a>
</div>


<div class="calendar calendar-list">
  <h2 style="text-align: center;">Dag för dag</h2>

  <ol class="days">
    {{#each days}}
        {{#ifeq month.name ../selectedMonth.month}}
        <li class = "{{#if isToday}}isToday{{/if}}
            {{#if redDay}}redDay{{/if}}
            {{#if helgDag}}helgDag{{/if}}
            {{#ifeq veckodag 'Lördag'}}weekend{{/ifeq}} {{#ifeq veckodag 'Söndag'}}weekend{{/ifeq}}">
            <div class="date">
            {{dayNumber}} {{#ifnoteq month.name ../selectedMonth.month}}{{month.name}}{{/ifnoteq}}
            </div>
            <div class="holiday">
            {{helgdag}}
            {{helgdagsafton}}
            </div>
            <div class="namedays">
            {{#each namnsdag}}
                <span>{{this}}</span>{{#unless @last}} &amp; {{/unless}}
            {{/each}}
            </div>
            <div class="themedays">
                {{#each themeDays}}
                    <p>{{this}}</p>
                {{/each}}
            </div>
        </li>
      {{/ifeq}}
    {{/each}}
    <li>
  </ol>
</div>

<menu class="menu-bar menu-bar-bottom" style="background-color: white;" >
    <a style="float: left" class="button" href="/{{prevQueryString}}">Föregående månad</a>
    <a style="float: right" class="button" href="/{{nextQueryString}}">Nästa månad</a>
</menu>
